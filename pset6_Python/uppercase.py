before = input("Before: ")
# print("After : ", end = "") # 因為後面需要打印 故修改end跑循環持續打印
# for c in before:
     # print(c.upper(), end = "") # 修改end避免每打印一次便換行
# print() # 最後打印一行當換行

print(f"After : {before.upper()}")
