#include <stdio.h>
#include <cs50.h>
#include <string.h>

// 循環 strlen 用法

int main(void)
{
    string s = get_string("Input: ");
    printf("Output: ");
    // int length = strlen(s);
    for (int i = 0; n = strlen(s); i < n; i++)  // strlen(s) 為輸入的字串 ex:David
    {
        printf("%c",s[i]); // 字串裡的字元 ex:01234
    }
    printf("\n");
}
